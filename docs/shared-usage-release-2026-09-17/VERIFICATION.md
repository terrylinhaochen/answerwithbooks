# Verification

Local results before release: 12 provider-price, workspace-grant, authorization, reservation and lost-response settlement tests pass against PostgreSQL; all 65 AWB backend tests pass, including all collection modes sharing holds with ordinary fixed-price AWB tools. An authenticated OpenAI gpt-5.4-mini request returned token usage. X Baseten account/affiliate discovery previously returned real affiliated users and original posts using the updated bearer token.

Release builds and authenticated cross-service pilot are pending. These local results are not production end-to-end proof. Production payments remain disabled until separately configured and verified.
